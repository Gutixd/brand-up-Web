---
// Ruta propia para que esta landing compile solo su CSS (ver bespokeWork.ts).
import WorkRegalonPet from '../../../components/pages/WorkRegalonPet.astro';
import { getProject } from '../../../data/projects';

const project = getProject('regalon-pet')!;
---

<WorkRegalonPet locale="en" project={project} />
