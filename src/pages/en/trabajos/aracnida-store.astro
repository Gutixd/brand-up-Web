---
// Ruta propia para que esta landing compile solo su CSS (ver bespokeWork.ts).
import WorkAracnida from '../../../components/pages/WorkAracnida.astro';
import { getProject } from '../../../data/projects';

const project = getProject('aracnida-store')!;
---

<WorkAracnida locale="en" project={project} />
