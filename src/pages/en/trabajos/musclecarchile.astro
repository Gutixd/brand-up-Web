---
// Ruta propia para que esta landing compile solo su CSS (ver bespokeWork.ts).
import WorkMuscleCar from '../../../components/pages/WorkMuscleCar.astro';
import { getProject } from '../../../data/projects';

const project = getProject('musclecarchile')!;
---

<WorkMuscleCar locale="en" project={project} />
